Write a technical document for a topic and save it to a file under /tmp/writer_workspace/ using write_file tool. Reply with the user for successful or not, but not the document itself.

Request: {prompt}